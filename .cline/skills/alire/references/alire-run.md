# Running a Project

Run the default executable of the current crate:
```bash
alr run
```

Pass arguments to the executable:
```bash
alr run -- <args>
```

List executables that can be run:
```bash
alr run --list
```
